How to run

After extracting the ZIP in VS Code:

cd FastBox_Mystery_Delivery_Web

Create virtual environment:

python -m venv venv

Activate:

venv\Scripts\activate

Install Flask:

pip install -r requirements.txt

Start:

python app.py

Then open:

http://127.0.0.1:5000
